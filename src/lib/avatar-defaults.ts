/**
 * Default avatar outfit colors.
 * These match the Prisma schema defaults and are used across the app
 * so that colors stay in sync between DB, API, components, and profile UI.
 */
export const DEFAULT_OUTFIT_COLORS = {
  shirt: "#3D5A45",
  pants: "#2D4635",
  hair: "#4A3728",
  skin: "#E8C4A0",
} as const;
